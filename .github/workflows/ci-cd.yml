name: CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
      - uses: actions/checkout@v4
      - name: Set up Node.js
      - uses: actions/setup-node@v2
        with:
          node-version: '20'
      - name: Install dependencies
      - uses: yarn install
      - name: Build the app
      - uses: yarn build
      - name: Deploy to server
      - uses: scp -r build/* root@212.192.9.251:/var/www/react-app